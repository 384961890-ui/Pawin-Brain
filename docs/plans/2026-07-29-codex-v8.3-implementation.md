# Pawin Brain v8.3 Codex Host Implementation Plan

1. Scaffold the repo-local Codex marketplace and `pawin-brain` plugin.
2. Add an explicit public-runtime allowlist and deterministic bundle builder.
3. Implement a non-destructive runtime bootstrapper and generic first-run data.
4. Implement the Codex hook router and event mapping.
5. Add the plugin manifest, hook manifest, doctor command, and install docs.
6. Add unit, parity, bundle-integrity, and de-identification tests.
7. Update README and changelog with Claude/Codex/ZCode entry points.
8. Validate the plugin and run the complete public v8.3 test suite.
9. Rename the GitHub repository to `Pawin-Brain`, push the release commit, and
   verify a clean-clone installation path.
10. Update the production Brain page, back up the remote file, upload only the
    page asset, run `nginx -t`, and verify desktop/mobile online behavior.

